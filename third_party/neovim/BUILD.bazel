load(":macros.bzl", "run_neovim")

run_neovim(
    name = "neovim",
    args = [
        "--headless",
        "$(location :textfile.txt)"
    ],
    data = [
        "//plugin/nvim:scripts",
        "//plugin/nvim/lua/pcc:scripts",
        ":textfile.txt",
    ],
)

